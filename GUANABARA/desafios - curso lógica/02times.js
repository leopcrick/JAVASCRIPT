var times = ['Vasco', 'Flamengo', 'Botafogo']

for ( i=0 ; i<times.length ; i++){
    for( c=0 ; c<times.length ; c++){
        if (times[i] == times[c]){

        } else {
            console.log(`${times[i]} x ${times[c]}`)
        }
    }
}